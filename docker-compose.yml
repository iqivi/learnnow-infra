version: '3.9'

services:

  nginx:
    image: nginx:1.25.3-alpine
    container_name: dev_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf/devops.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend

  backend:
    build:
      context: ../backend
    container_name: dev_backend
    restart: unless-stopped

  frontend:
    build:
      context: ../web-app
    container_name: dev_frontend
    restart: unless-stopped
